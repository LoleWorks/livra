{
  "cli": {
    "version": ">= 12.0.0"
  },
  "build": {
    "preview": {
      "android": {
        "buildType": "apk",
        "prebuildCommand": "npx expo prebuild --clean && sed -i '/ndk.dir/d' android/local.properties"
      }
    },
    "production": {
      "android": {
        "buildType": "app-bundle",
        "prebuildCommand": "npx expo prebuild --clean && sed -i '/ndk.dir/d' android/local.properties"
      }
    }
  }
}
